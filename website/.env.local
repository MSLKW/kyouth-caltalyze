# Copy to .env.local and set to your backend's origin.
# Leave blank if the backend is reverse-proxied under the same origin as this app,
# or configure server.proxy in vite.config.js during development.
# VITE_API_BASE_URL=http://localhost:8000
